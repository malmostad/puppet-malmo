<% require 'securerandom' -%>
development:
  secret_key_base: <%= SecureRandom.hex(64) %>
test:
  secret_key_base: <%= SecureRandom.hex(64) %>
local_test:
  secret_key_base: <%= SecureRandom.hex(64) %>
production:
  secret_key_base: <%= SecureRandom.hex(64) %>
